# MSG Buyer - Парсер автозапчастей

Проект для автоматического парсинга данных о запчастях с сайта rrr.lt и сохранения их в базу данных PostgreSQL.

## 🚀 Возможности

- **Парсинг списка товаров** - извлечение базовой информации о товарах со страницы категории
- **Детальный парсинг** - извлечение полной информации о каждом товаре:
  - Описание товара (коды производителя, OEM, состояние)
  - Детали автомобиля (марка, модель, год, VIN и др.)
  - Информация о продавце (название, страна, рейтинг)
  - Изображения товара
- **Сохранение в БД** - автоматическое сохранение всех данных в PostgreSQL (Railway.com)
- **Обновление данных** - автоматическое обновление существующих товаров при повторном парсинге

## 📁 Структура проекта

```
msg_buyer/
├── main.py                      # Точка входа в программу
├── requirements.txt             # Зависимости проекта
├── .env                         # Переменные окружения (не в Git)
├── sources/                     # Основной код проекта
│   ├── classes/                 # Модели данных
│   │   └── product.py           # Класс Product (бизнес-модель)
│   ├── database/                # Работа с базой данных
│   │   ├── models.py            # SQLAlchemy модели
│   │   ├── repository.py        # Репозиторий для работы с БД
│   │   └── config.py            # Конфигурация подключения к БД
│   ├── parsers/                 # Парсеры HTML
│   │   └── rrr/
│   │       └── steering_rack_parser.py  # Парсер для steering rack
│   └── scrapers/                # Скраперы (Selenium)
│       ├── base_scraper.py      # Базовый класс скрапера
│       └── rrr_scraper.py       # Скрапер для rrr.lt
└── docs/                        # Документация и тестовые скрипты
```

## 🛠 Технологии

- **Python 3.x**
- **Selenium** - автоматизация браузера для работы с динамическим контентом
- **BeautifulSoup4** - парсинг HTML
- **SQLAlchemy** - ORM для работы с базой данных
- **PostgreSQL** - база данных (развернута на Railway.com)
- **Edge WebDriver** - драйвер для браузера Microsoft Edge

## 📦 Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd msg_buyer
```

### 2. Создание виртуального окружения

```bash
python -m venv .venv
```

### 3. Активация виртуального окружения

**Windows (PowerShell):**
```bash
.venv\Scripts\Activate.ps1
```

**Windows (CMD):**
```bash
.venv\Scripts\activate.bat
```

**Linux/Mac:**
```bash
source .venv/bin/activate
```

### 4. Установка зависимостей

```bash
pip install -r requirements.txt
```

## ⚙️ Настройка

### 1. Настройка базы данных

1. Создайте PostgreSQL базу данных на [Railway.com](https://railway.com)
2. Получите `DATABASE_PUBLIC_URL` из настроек Railway
3. Создайте файл `.env` в корне проекта:
   ```env
   DATABASE_PUBLIC_URL=postgresql://user:password@host:port/database
   ```

**Важно:** Файл `.env` уже добавлен в `.gitignore` и не будет закоммичен в Git.

### 2. Проверка подключения к БД

После настройки `.env` файла таблицы создадутся автоматически при первом запуске.

## 🎯 Использование

### Запуск парсинга

```bash
python main.py
```

Программа выполнит следующие действия:

1. **Инициализация** - подключение к БД и инициализация скрапера
2. **Парсинг списка** - получение списка товаров со страницы steering rack
3. **Обработка товаров** - для первых 3 товаров:
   - Переход на детальную страницу
   - Извлечение всех данных (описание, детали авто, продавец, изображения)
   - Сохранение в базу данных
4. **Статистика** - вывод результатов работы

### Что парсится

#### Базовая информация (со списка товаров):
- `part_id` - уникальный идентификатор товара
- `code` - код товара (SKU)
- `price` - цена
- `url` - ссылка на товар
- `source_site` - источник (rrr.lt)
- `category` - категория (steering-rack)

#### Детальная информация (со страницы товара):

**Item Description:**
- `manufacturer_code` - код производителя
- `oem_code` - OEM код
- `other_codes` - другие коды
- `condition` - состояние товара

**Car Details:**
- `make` - марка автомобиля
- `series` - серия
- `model` - модель
- `year` - год выпуска
- `engine_capacity` - объем двигателя
- `gearbox_code` - код коробки передач
- `mileage` - пробег
- `vin_code` - VIN код
- И другие поля

**Seller Info:**
- `name` - название продавца
- `country` - страна
- `rating` - рейтинг (Top Seller и др.)
- `stars` - количество звезд

**Images:**
- Список URL всех изображений товара

## 🗄 База данных

### Структура таблицы `products`

- `id` - автоинкремент (PRIMARY KEY)
- `part_id` - уникальный идентификатор (UNIQUE, INDEX)
- `code` - код товара (INDEX)
- `price` - цена
- `url` - ссылка на товар
- `source_site` - источник
- `category` - категория
- `item_description` - JSONB (описание товара)
- `car_details` - JSONB (детали автомобиля)
- `seller_info` - JSONB (информация о продавце)
- `images` - JSONB (массив URL изображений)
- `created_at` - дата создания
- `updated_at` - дата обновления

### Методы репозитория

```python
from sources.database.repository import ProductRepository
from sources.database.config import get_database_url

repository = ProductRepository(get_database_url())

# Сохранение товара (создает или обновляет)
repository.save(product)

# Удаление по part_id
repository.delete_by_part_id("70417637")

# Удаление по code
repository.delete_by_code("OVP35834")

# Поиск по part_id
product = repository.find_by_part_id("70417637")

# Поиск по code
product = repository.find_by_code("OVP35834")
```

## 📝 Зависимости

- `selenium>=4.15.0` - автоматизация браузера
- `webdriver-manager>=4.0.0` - управление драйверами
- `beautifulsoup4>=4.12.0` - парсинг HTML
- `sqlalchemy>=2.0.0` - ORM для работы с БД
- `psycopg2-binary>=2.9.0` - драйвер PostgreSQL
- `python-dotenv>=1.0.0` - загрузка переменных окружения

## 🔧 Архитектура

Проект использует архитектуру с разделением ответственности:

- **Scrapers** - получение HTML через Selenium
- **Parsers** - извлечение данных из HTML
- **Classes** - бизнес-модели данных (Product)
- **Database** - работа с базой данных (SQLAlchemy)

Это позволяет легко расширять проект для парсинга других категорий товаров или других сайтов.

## 📊 Текущий статус

✅ Парсинг списка товаров  
✅ Детальный парсинг товаров  
✅ Сохранение в PostgreSQL  
✅ Обновление существующих товаров  
✅ Извлечение всех необходимых полей  

## 🚧 В планах

- Парсинг всех страниц категории
- Парсинг других категорий товаров
- Периодическое обновление данных
- Обработка ошибок и повторные попытки
- Логирование

## 📄 Лицензия

Проект в разработке
